# Ram-Prakash-Tudu
This project builds a **hybrid fraud detection system** using supervised and unsupervised machine learning on a credit card transaction dataset. **Isolation Forest** detects anomalies and its scores are combined with original features. **Logistic Regression and Random Forest** models are then used to predict fraudulent transactions and performance.
